# iGetToken 部署清单（Cloudflare Pages）

> 前置状态：站点已构建验证通过（`npm run build` → 27 页），本地预览正常。
> 目标：推 GitHub → Cloudflare Pages 自动构建 → 绑定正式域名。全程免费，免备案。

---

## 第 1 步：本地初始化 Git 仓库

```bash
cd F:\zcode\igetoken
git init
git add .
git commit -m "init: iGetToken MVP 站点"
```

> `.gitignore` 已配好（node_modules / dist / .astro 不会入库）。
> 这一步我可以直接帮你执行，说一声即可。

## 第 2 步：创建 GitHub 仓库并推送

1. 登录 GitHub → New repository
2. 仓库名建议 `igetoken`，**建议选 Public**（配合 awesome 仓库策略做增长；公开仓库的 GitHub Actions 分钟数也不限量，M4 的信源监控零成本）
3. 不要勾选 README / .gitignore / License 初始化（本地已有）
4. 推送：

```bash
git remote add origin https://github.com/<你的用户名>/igetoken.git
git branch -M main
git push -u origin main
```

## 第 3 步：Cloudflare Pages 创建项目

1. 登录 [dash.cloudflare.com](https://dash.cloudflare.com) → 左侧 **Workers & Pages** → Create → **Pages** → Connect to Git
2. 授权 GitHub，选择 `igetoken` 仓库
3. 构建配置：
   - Framework preset：**Astro**
   - Build command：`npm run build`
   - Build output directory：`dist`
4. Save and Deploy，约 1-2 分钟后得到 `igetoken.pages.dev` 预览地址

此后每次 `git push` 自动构建上线，无需手动操作。

## 第 4 步：绑定正式域名

前提：域名 DNS 已托管到 Cloudflare（NS 已改为 Cloudflare 的地址）。

1. Pages 项目 → **Custom domains** → Set up a custom domain
2. 输入 `igetoken.com`，按提示确认（域名在同一 Cloudflare 账号下会自动添加 CNAME 记录）
3. 再加一个 `www.igetoken.com`，同样指向 Pages 项目
4. 等待证书自动签发（通常几分钟，最长 24 小时），HTTPS 生效即完成

## 第 5 步：上线后收尾

- [ ] 确认正式域名就是 `igetoken.com`（`astro.config.mjs` 里的 `site` 已按此配置，影响 sitemap / canonical / RSS 链接；**如果最终用了其他域名，必须同步改这里并重新部署**）
- [ ] 访问 `https://igetoken.com/rss.xml` 与 `/sitemap-index.xml` 确认可读
- [ ] Cloudflare 后台开启 **Web Analytics**（免费无 cookie，把脚本加进 `src/layouts/Base.astro` 的 `<head>`）
- [ ] [Google Search Console](https://search.google.com/search-console) 提交 `https://igetoken.com/sitemap-index.xml`（顺带提交 Bing Webmaster，国内搜索引擎收录各自提交）
- [ ] 手机打开全站过一遍响应式

## 验收清单

| 检查项 | 标准 |
|--------|------|
| 首页与全部详情页 | HTTPS 可访问，无混合内容警告 |
| git push → 上线 | push 后 1-2 分钟内 Pages 构建完成 |
| RSS / sitemap | `/rss.xml`、`/sitemap-index.xml` 返回 200 |
| 404 页 | 随意输入不存在路径显示自定义 404 |
| 移动端 | 页签筛选、汉堡菜单可用 |

---

## 日常发布流程（上线后）

改内容 → commit → push，1-2 分钟自动上线。手机登 GitHub 网页版编辑 `src/data/deals.json` 同样可行（模板见 README）。
